trigger: none
pool:
  name: infrapool
steps:
- task: TerraformInstaller@1
  displayName: terraforminstaller
  inputs:
     terraformVersion: 'latest'

- task: TerraformTask@5
  displayName: terraform init
  inputs:
    provider: 'azurerm'
    command: 'init'
    workingDirectory: '$(System.DefaultWorkingDirectory)/dev'
    backendServiceArm: 'yuvaconnection'
    backendAzureRmStorageAccountName: 'vmstorage1707'
    backendAzureRmContainerName: 'vminerconta'
    backendAzureRmKey: 'pak.terraform.tfstate'

# trigger: none

# pool:
#   name: infrapool

# steps:
# # 1️⃣ Terraform install
# - task: TerraformInstaller@1
#   displayName: Install Terraform
#   inputs:
#     terraformVersion: 'latest'


# # 2️⃣ Azure login + terraform init (PowerShell)
# - task: AzureCLI@2
#   displayName: Terraform Init using PowerShell
#   inputs:
#     azureSubscription: 'yuvaconnection'   # service connection
#     scriptType: 'ps'
#     scriptLocation: 'inlineScript'
#     workingDirectory: '$(System.DefaultWorkingDirectory)/dev'
#     inlineScript: |
#       terraform --version

#       terraform init `
#         -backend-config="storage_account_name=vmstorage1707" `
#         -backend-config="container_name=vminerconta" `
#         -backend-config="key=pak.terraform.tfstate"
